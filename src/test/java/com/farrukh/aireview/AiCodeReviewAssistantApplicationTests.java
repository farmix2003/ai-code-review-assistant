package com.farrukh.aireview;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Integration test class for the AI Code Review Assistant Spring Boot application.
 * Tests that the application context loads correctly and all beans are properly initialized.
 * Uses @SpringBootTest annotation to load the full application context for testing.
 */
@SpringBootTest
class AiCodeReviewAssistantApplicationTests {

    /**
     * Test method that verifies the Spring Boot application context loads without errors.
     * This is a sanity check to ensure all beans are properly configured and initialized.
     */
    @Test
    void contextLoads() {
    }

}
